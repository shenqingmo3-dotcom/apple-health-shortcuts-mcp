# 安全说明

这个小项目会接触个人健康数据。请把它当作一本带锁的私人日记。

- 不要把 `my-keys.local` 上传到 GitHub，也不要把两把钥匙发到群里。
- `UPLOAD_KEY` 只放进自己的 iPhone 快捷指令。
- `MCP_ACCESS_KEY` 只放进自己信任的 AI 软件。
- 公开截图前，遮住 Worker 地址、数据库编号和两把钥匙。
- 怀疑钥匙泄露时，重新生成钥匙并再次执行 `wrangler secret put`。
- 不要把本项目用于急救、诊断或替代医生。

如发现安全问题，请不要公开贴出真实健康数据、网址或钥匙。请在仓库里提交一条不含隐私的安全说明。
